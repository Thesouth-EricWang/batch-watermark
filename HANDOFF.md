# 交接说明

给「下一个接手的自己」或「下一次开新对话」看：当前状态、下一步、雷区。

## 当前状态

桌面版功能完整、可用。已实现：

- 批量给文件夹里的图片加标志，四角可选
- 尺寸/留白/不透明度，滑块 + 数字框双向联动
- 深色底自动适配（校徽垫圆底 + 文字反白），含圆底浓度/大小、深浅判定阈值
- 单张增强：水印黑白覆盖、描边（白/黑/双色）、毛玻璃底板，**按图独立记录**
- 三层默认：全局 → 本文件夹 → 本张
- 预览：滚轮/按钮缩放（视口裁剪，32 倍上限）、拖动、双击复原、翻页复原、相邻图预解码
- 查看原图：新窗口，后台线程渲染，内部降到 2200px
- 导出弹窗：输出位置、格式（原格式/JPEG/PNG/WebP）、质量
- 顶栏菜单：文件 / 设置 / 关于
- 配置按文件夹持久化；日志轮转

## 已知问题（待修）

1. **描边在小尺寸下比标称略细**：超采样 2 倍后缩回，边缘按比例会损失一点（设 6px 出约 5px）。要更准就把超采样提到 3～4 倍，代价是多一点耗时。
2. **预览与导出在极细纹理背景下略有差异**：预览基图是缩略图，细节已被平均。差异实测约 4/255，可忽略；要更准可提高 `PREVIEW_MAX`。
3. **「查看原图」内部上限 2200px**：对超大图不是 1:1。若需要真正 1:1，得改成按需加载局部区域。

## 下一步（可做）

- 打包成便携版（PyInstaller 或换原生）。当前是「本机装了 Python 才能跑」，要给别人用得先打包。
  - 注意：打包预判——`tkinter + Pillow + CustomTkinter` 打包后体积不小，且 `--windowed` 下需要真的枚举窗口验证是否弹出。
- 支持多标志切换（现在只认 `logo_trans.png` 一个）
- 导出时按子文件夹保留目录结构

## 雷区（改之前先读）

- **`watermark_core.py` 不要 import tkinter**。它是可以脱离界面单测的，保持这个性质。
- **改保存相关逻辑**：任何「切换作用域」（换文件夹、换输出位置）之前必须先落盘；关窗口前必须 flush。
- **改右栏布局**：面板宽度是钉死的，别把控件设成跟随内容自适应，否则会横向跳动。
- **跨线程**：子线程只往队列里放结果，绝不在子线程里调 Tk。
- **`.bat` 只能是 ASCII**；含中文的批处理会被 cmd 按 GBK 解析错乱。
- **`outline_w` 是比例不是像素**（0.08 = 标志高度的 8%）。传成 8 会让膨胀循环跑几百次、直接卡死（核心已做钳制，但别依赖它）。

## 验证方式（这些坑都是「看起来成功」）

| 要确认的事 | 办法 |
|---|---|
| 界面真的起来 | 用 pythonw 起，看有没有生成 `启动错误.log` |
| 配置真的保存 | 改几项 → 关窗 → 重开同文件夹，看是否恢复 |
| 切文件夹不串 | 在 A 改完立刻切到 B，检查 settings.json 里 A 的值对不对 |
| 导出真的对 | 看输出图片尺寸是否等于原图；参数改了输出是否真的变 |
| 性能 | 拖滑杆测单次重渲耗时（目标 < 80ms）；拖预览测重绘（目标 < 40ms） |

## 开发环境

- Python 3.14（本机路径 `C:\Users\ytwbc\AppData\Local\Python\bin\python.exe`）
- Pillow 12.3、CustomTkinter 6.0
- 启动：`启动.bat`（内部用 `pythonw`）
